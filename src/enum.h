#ifndef ENUM_H
#define ENUM_H

// Defining enum Jour
enum Jour
{
    LUNDI,
    MARDI,
    MERCREDI,
    JEUDI,
    VENDREDI,
    SAMEDI,
    DIMANCHE
};

// Defining enum Specialite
enum Specialite
{
    SANS,
    JARDINAGE,
    MENUISERIE,
    ELECTRICITE,
    MECANIQUE,
    MUSIQUE
};

// Defining enum Temps
enum Temps
{
    PARTIEL,
    PLEIN
};

// Defining enum Competence
enum Competence
{
    SIGNES,
    CODAGE
};

#endif